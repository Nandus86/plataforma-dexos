# Plano de Funcionalidades - Página de Matrículas

## Novas Funcionalidades Solicitadas

### 1. Campo de Pesquisa
- Adicionar input para filtrar alunos por nome ou email na listagem de matrículas
- O filtro deve enviar parâmetro `search` para o backend

### 2. Ordenação
- Adicionar dropdown para selecionar ordenação por:
  - Estudante (ordem alfabética)
  - Curso (ordem alfabética)
  - Ano/Período
  - Status
- O backend deve aceitar parâmetro `order_by` para definir a ordenação

---

## Implementação

### Backend (`backend/app/api/academic.py`)

**Alterações necesarias:**
1. Adicionar parâmetros `search` e `order_by` na função `list_enrollments` (linha 34-43)
2. Implementar filtro de pesquisa por nome/email do aluno
3. Implementar ordenação dinâmica baseada no parâmetro `order_by`

```python
@router.get("/enrollments/", response_model=EnrollmentListResponse)
async def list_enrollments(
    ...
    search: Optional[str] = None,
    order_by: Optional[str] = "created_at",
    ...
):
    # Filtro de busca por nome/email
    if search:
        conditions.append(or_(
            Student.name.ilike(f"%{search}%"),
            Student.email.ilike(f"%{search}%")
        ))
    
    # Ordenação dinâmica
    order_column = {
        "student_name": Student.name,
        "course_name": Course.name,
        "year": Enrollment.year,
        "status": Enrollment.status,
    }.get(order_by, Enrollment.created_at.desc())
    
    if order_by in ["status", "year"]:
        query = query.order_by(order_column)
    else:
        query = query.order_by(order_column.asc())
```

### Frontend (`frontend/src/app/features/enrollments/enrollments.component.ts`)

**Alterações necesarias:**
1. Adicionar variável `searchText` e `sortBy` no componente
2. Adicionar campo de input de pesquisa no template (linha ~98)
3. Adicionar dropdown de ordenação no template
4. Atualizar `loadEnrollments()` para enviar os novos parâmetros

---

## Arquivos a Modificar

| Arquivo | Alteração | Linha |
|---------|-----------|-------|
| `backend/app/api/academic.py` | Adicionar search e order_by | 34-43, 61-70 |
| `frontend/src/app/features/enrollments/enrollments.component.ts` | Adicionar pesquisa e ordenação | 98, 186 |